if (!cc.sys.isNative) {
    var crashReport = crashReport || {};

    crashReport.report = function(message) {
    }

    crashReport.log = function(message) {
    }

    crashReport.logcat = function(message) {
    }
}
